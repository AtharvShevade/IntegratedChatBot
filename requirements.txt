# requirements.txt

# ── Core framework ──────────────────────────────────────────────────────────

fastapi>=0.111.0
uvicorn[standard]>=0.29.0       # includes httptools + uvloop for performance

# ── Database ─────────────────────────────────────────────────────────────────

oracledb>=2.2.0                 # Oracle DB driver (thin mode — no Oracle Client needed)

# ── HTTP client (async calls to Ollama + the Whisper STT service) ───────────

httpx>=0.27.0

# ── File upload support (multipart/form-data) ────────────────────────────────

python-multipart>=0.0.9

# ── Data validation ──────────────────────────────────────────────────────────

pydantic>=2.7.0

# ── Environment / configuration ──────────────────────────────────────────────

python-dotenv>=1.0.0

# ── Semantic intent classification ───────────────────────────────────────────

# sentence-transformers and numpy removed: intent is now extracted by the LLM

# (Ollama / phi3-mini). Install whichever model you prefer via: 


# ollama pull phi3:mini          # ~2 GB, fastest

# ollama pull qwen2.5-coder:latest      # ~6 GB, balanced


# ── Flexible date parsing ────────────────────────────────────────────────────

python-dateutil>=2.9.0          # fuzzy date parsing

# ── Fuzzy keyword matching ───────────────────────────────────────────────────

rapidfuzz>=3.0.0                # typo-tolerant keyword escape guards in agent

# ── XBRL variance analysis ───────────────────────────────────────────────────

arelle-release                  # Arelle XBRL toolkit
pandas>=2.0.0                  # variance DataFrame / CSV export (optional)

# ── SQL Agent (FAISS + BM25 hybrid retrieval, BGE embeddings) ───────────────
# Drives the self-contained agent under backend/sql_agent/ (engine + prebuilt
# embeddings/ + config, no separate sql_agent/ checkout or build tooling).

sentence-transformers==2.7.0
faiss-cpu>=1.13.0               # bumped from >=1.7.4 — verify shipped .faiss files load under 1.13+ before deploying
numpy>=2.1.0                    # explicit pin — verify against arelle-release/pandas/scikit-learn before deploying
rank-bm25>=0.2.2                # lexical fusion signal in src/lexical_search.py, on by default (BM25_SIGNAL_WEIGHT=1.5)
scipy
scikit-learn
tqdm
regex
requests>=2.31.0                # sync Ollama calls in sql_generator / selector
PyYAML>=6.0                     # semantic_layer.yaml, business_dictionary.yaml (declared joins + metrics + aliases)

# ── XML / Parsing Utilities ──────────────────────────────────────────────────

lxml>=5.2.0
beautifulsoup4>=4.12.0

# ── Async / Performance Utilities ────────────────────────────────────────────

anyio>=4.4.0
aiofiles>=24.1.0

# ── Logging / Monitoring ─────────────────────────────────────────────────────

loguru>=0.7.2

# ── Optional JSON utilities ──────────────────────────────────────────────────

orjson>=3.10.0

# ── Retry / resilience ───────────────────────────────────────────────────────

tenacity>=8.2.3
